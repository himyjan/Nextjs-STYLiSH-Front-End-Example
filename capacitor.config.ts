import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'nextjs-stylish-front-end-example',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
