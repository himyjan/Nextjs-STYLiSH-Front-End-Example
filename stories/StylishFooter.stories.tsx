import React from 'react';

import Footer from '../components/Footer/Footer';

export default {
  title: 'Stylish/Footer',
  component: Footer,
};

const Template = (args) => <Footer />;

export const FooterUI = Template.bind({});
