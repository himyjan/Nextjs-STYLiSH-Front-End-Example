import React from 'react';

import Header from '../components/Header/Header';

export default {
  title: 'Stylish/Header',
  component: Header,
};

const Template = (args) => <Header />;

export const HeaderUI = Template.bind({});
